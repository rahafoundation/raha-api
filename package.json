{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "./scripts/buildTestAll.sh",
    "build": "(cd packages/shared && yarn build) & (cd packages/client && yarn build) & (cd packages/"
  }
}
